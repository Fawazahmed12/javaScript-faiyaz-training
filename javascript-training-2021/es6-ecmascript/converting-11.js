const products=
Array.from(document.querySelectorAll('.products'));
products
.filter(products=>parseFloat(products,innerHTML)<10)
.forEach(products=>products.getElementsByClassName.color='red');
console.log(products);